apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: relay-api
  namespace: relay
spec:
  selector:
    matchLabels:
      app: api
  endpoints:
    - port: http
      path: /metrics
      interval: 15s